Optional: drop a royalty-free lofi/acoustic track here as `soft-lofi-romance.mp3`.
This is just the local dev fallback — invitations created after ImageKit is
configured store their own uploaded track URL instead.
